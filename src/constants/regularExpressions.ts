export const translationPathRegExp = /ru(\/)?|en(\/)?/;

export const phoneRegExp = /^[+]?[(]?[0-9]{3}[)]?[-s.]?[0-9]{3}[-s.]?[0-9]{4,6}$/im;

export const digitsRegExp = /^[0-9]+$/;

export const expiryDateRegExp = /^(0[1-9]|1[0-2])\/?([0-9]{2})$/;

export const nameRegExp = /^[A-Za-z]+$/i;
